{{- if eq .chezmoi.os "windows" }}
{{- /* tmux, Nix, home-manager は Windows では動作しないため、対象から外す */}}
.config/tmux
.config/nix
.config/home-manager
{{- /* VS Code のユーザ設定は AppData 側を適用する */}}
.config/Code
{{- end }}

{{- if ne .chezmoi.os "windows" }}
{{- /* Windows 専用のターゲット */}}
AppData
{{- /* WezTerm は Windows 上でのみ用いる */}}
.config/wezterm
{{- end }}
